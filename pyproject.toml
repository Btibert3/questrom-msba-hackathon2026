[project]
name = "questrom-msba-hackathon2026"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "prefect>=3",
    "duckdb",
    "langchain",
    "langchain-openai",
    "langsmith",
    "pandas",
    "httpx",
    "python-dotenv",
    "modal",
    "streamlit",
    "mlb-statsapi",
]
